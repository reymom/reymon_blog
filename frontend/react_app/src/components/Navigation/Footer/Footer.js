import React from 'react';

import SocialMedia from '../SocialMedia/Icons';
import classes from './Footer.module.css';

const Footer = (props) => (
    <footer className={classes.Footer}>
        <SocialMedia />
    </footer>
);

export default Footer;
